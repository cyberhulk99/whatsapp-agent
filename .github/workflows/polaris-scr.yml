name: Polaris SCR

on:
  push:
    branches: [main]
  pull_request:

jobs:
  scr:
    uses: tripsure/polaris-workflows/.github/workflows/polaris-scr.yml@main
    with:
      app_key: whatsapp-agent
      polaris_url: http://localhost:8082
